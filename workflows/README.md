```md
# Workflows (n8n)

Examples to be added separately (SoC):
- `daily_digest.json` — fetches sources, summarizes, emails update.
- `encrypted_backups.json` — dumps DB, encrypts with BYOK passphrase, prunes.
- `meeting_minutes.json` — audio → transcript → summary → email.
- `personal_rag.json` — chat to RAG over `/docs`.
- `image_variations.json` — batch variations via ComfyUI.

Import: n8n UI → Import Workflow → choose JSON file.
