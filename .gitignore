# ─────────────────────────────────────────────────────────────
# Sushi Kitchen – .gitignore (curated)
# Keep this file tracked. Do NOT list `.gitignore` here.
# Last updated: 2025-09-02
# ─────────────────────────────────────────────────────────────

# ----- CRITICAL: Secrets / env -----
.env
.env.*
*.env
*.env.*
*.env.local

# ----- Models / large artifacts -----
models/
models/**

# ----- Local data volumes (persisted state; never commit) -----
supabase/volumes/
n8n_data/
portainer_data/
weaviate-data/
qdrant_storage/
neo4j/data/
redis/data/
prometheus_data/
anythingllm_storage/
arangodb/
langfuse_clickhouse_data/
langfuse_minio_data/
langfuse_postgres_data/
memgraph/
weaviate-data/
comfyui/output/

# ----- IDE / OS caches (but keep shared editor configs we ship) -----
.idea/
# We intentionally keep .vscode/ committed because we ship tasks & launch configs
# .vscode/         ← do NOT ignore; it's part of the repo
# We intentionally keep .devcontainer/ committed
# .devcontainer/   ← do NOT ignore; it's part of the repo
.DS_Store
Thumbs.db
*.log
*.tmp
*~
.cache/
.tmp/
.vscode-test/

# ----- Python -----
__pycache__/
*.py[cod]
*$py.class
.Python
.pytest_cache/
.coverage
.coverage.*
htmlcov/
dist/
build/
*.egg-info/
.eggs/
.venv
venv/
ENV/
env/
pip-log.txt
pip-delete-this-directory.txt
.mypy_cache/
.pytype/
.pyre/
.ruff_cache/

# ----- Node / JS -----
node_modules/
bower_components/
web_modules/
.npm/
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.stylelintcache
.parcel-cache
.next/
.nuxt/
.svelte-kit/
coverage/
.nyc_output/
dist/
build/
*.tsbuildinfo

# ----- Containers / Docker -----
.docker/
docker-volumes/
.docker-sync/
docker/data/
docker/logs/
# allow official compose files; only ignore local overrides
docker-compose.override.yml
docker-compose.local.yml

# IMPORTANT: do NOT ignore our compose files or CI!
# !compose/docker-compose.*.yml
# !.github/
# Keep lockfiles (better reproducibility)
# package-lock.json
# yarn.lock

# ----- Misc tools -----
.site/
docs/_build/
.ipynb_checkpoints
*.swp
*.swo
*.swn

# ----- System files -----
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.apdisk
# Linux
.fuse_hidden*
.directory
.Trash-*
.nfs*
